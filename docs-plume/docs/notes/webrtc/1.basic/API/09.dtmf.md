---
title: WebRTC DTMF 完全指南
createTime: 2026/08/18 18:21:25
permalink: /webrtc/1.basic/ueyqpob3/
---
# WebRTC DTMF 完全指南

> 双音多频信号 — 与传统电话网络交互

---

## 概述

DTMF（Dual-Tone Multi-Frequency）允许 WebRTC 客户端与传统电话网络（PSTN）或 VoIP 服务交互。

**限制**：DTMF 不能在两个 WebRTC 设备之间使用，因为 WebRTC 没有接收 DTMF 码的机制。

---

## 核心接口

| 接口 | 职责 |
|------|------|
| `RTCDTMFSender` | 发送 DTMF 音调 |
| `RTCDTMFToneChangeEvent` | 音调变化事件 |

---

## 工作流程

```
RTCDTMFSender
    │
    ├── insertDTMF(tones)     // 发送 DTMF 序列
    ├── toneBuffer             // 待发送的音调队列
    └── ontonechange = (ev)   // 监听此事件
              │
              └─→ RTCDTMFToneChangeEvent
                      │
                      └── tone: "1" | "2" | "" (结束)
```

---

## RTCDTMFSender

> 发送 DTMF 音调

### 获取方式

```js
const dtmfSender = audioTrackSender.dtmf;
```

### 属性

#### canInsertDTMF

是否支持 DTMF。

```js
dtmfSender.canInsertDTMF  // boolean
```

#### toneBuffer

当前排队待发送的 DTMF 音调列表（已播放的不包含在内）。

```js
dtmfSender.toneBuffer  // string
```

**格式**：
- 数字：`0-9`
- 字母：`A-D`（大写）
- 特殊键：`#`、`*`
- 逗号：`,` 表示暂停 2 秒（如 `"12,34"` 表示发 1、2，暂停 2 秒，再发 3、4）

### 方法

#### insertDTMF()

发送 DTMF 音调到远程对等端。

```js
dtmfSender.insertDTMF(tones);
dtmfSender.insertDTMF(tones, duration);
dtmfSender.insertDTMF(tones, duration, interToneGap);
```

**参数**：
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `tones` | string | - | DTMF 字符序列 |
| `duration` | number | 100ms | 每个音调的持续时间（40~6000ms） |
| `interToneGap` | number | 70ms | 音调之间的间隔（最小 30ms） |

**异常**：
- `InvalidStateError`：轨道已停止或处于只读/非活动状态
- `InvalidCharacterError`：包含无效 DTMF 字符

---

## RTCDTMFToneChangeEvent

> 音调变化事件

### 构造函数

```js
new RTCDTMFToneChangeEvent(type)
new RTCDTMFToneChangeEvent(type, options)
```

### 属性

#### tone

当前播放的 DTMF 字符，或空字符串表示所有音调已播放完毕。

```js
event.tone  // string
```

**返回值**：
| 值 | 含义 |
|----|------|
| `"0"-"9"` | 电话按键数字 |
| `"A"-"D"` | DTMF 标准键 |
| `"#"` / `"*"` | 特殊键 |
| `""` | 所有音调已播放完毕 |

---

## DTMF 字符表

| 字符 | 说明 |
|------|------|
| `0-9` | 电话按键数字 |
| `A-D` | DTMF 标准键（大多数电话上没有） |
| `#` | 井号键 |
| `*` | 星号键 |
| `,` | 暂停 2 秒 |

---

## 使用示例

### 基本用法

```js
// 获取 DTMF 发送器
const dtmfSender = sender.dtmf;

// 检查是否支持 DTMF
if (dtmfSender.canInsertDTMF) {
  // 发送 DTMF：1、2，暂停 2 秒，然后 3、4
  dtmfSender.insertDTMF('12,34', 500, 50);
  
  // 监听音调变化
  dtmfSender.ontonechange = (event) => {
    console.log('当前音调:', event.tone || '<结束>');
  };
}
```

### 实时显示音调

```js
dtmfSender.ontonechange = (ev) => {
  let tone = ev.tone;
  if (tone === "") {
    tone = "<none>";
  }
  document.getElementById("playingTone").innerText = tone;
};
```

### 追加音调

```js
// 追加新的音调到现有队列
const currentTones = dtmfSender.toneBuffer;
dtmfSender.insertDTMF(currentTones + '56');
```

### 取消发送

```js
// 清空缓冲区，取消所有待发送的音调
dtmfSender.insertDTMF('');
```

---

## 属性速查表

### RTCDTMFSender

| 属性/方法 | 类型 | 说明 |
|-----------|------|------|
| `canInsertDTMF` | boolean | 是否支持 DTMF |
| `toneBuffer` | string | 待发送的音调队列 |
| `insertDTMF(tones, duration, interToneGap)` | 方法 | 发送 DTMF 序列 |

### RTCDTMFToneChangeEvent

| 属性 | 类型 | 说明 |
|------|------|------|
| `tone` | string | 当前播放的音调字符 |

---

